<!DOCTYPE html>

<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Chess Game</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

    <style>
        .chessboard {
            display: grid;
            grid-template-columns: repeat(8, minmax(0, 1fr));
            grid-template-rows: repeat(8, minmax(0, 1fr));
            width: 400px;
            height: 400px;
            transform: rotate(0deg);
        }
        
        .piece.white {
            color: white;
            text-shadow: 0 0 2px black, 0 0 4px black, 0 0 6px black;
        }
        
        .piece.black {
            color: black;
            filter: drop-shadow(0 0 2px rgb(246, 236, 236));
        }
        
        .flipped {
            transform: rotate(180deg);
        }
        /* When board is flipped, keep pieces upright */
        
        .flipped .piece {
            transform: rotate(180deg);
        }
        
        .square {
            display: flex;
            align-items: center;
            justify-content: center;
        }
        
        .square.light {
            background-color: #ffffff;
        }
        
        .square.dark {
            background-color: #150f0f;
        }
        
        .piece {
            font-size: 36px;
            cursor: pointer;
            user-select: none;
        }
        
        .highlight {
            outline: 4px solid yellow !important;
            outline-offset: -4px;
            box-sizing: border-box;
        }
        /* simple layout */
        
        .controls {
            position: absolute;
            top: 20px;
            left: 20px;
            z-index: 10;
        }
        
        .controls button {
            margin-right: 8px;
            padding: 8px 12px;
            border-radius: 6px;
            cursor: pointer;
        }
        
        .checkmate-king {
            outline: 4px solid red !important;
            outline-offset: -4px;
            box-sizing: border-box;
        }
    </style>
</head>

<body class="bg-zinc-900 h-screen">
    <div class="controls text-white">
        <!-- Loading Overlay -->
        <div id="loadingOverlay" style="display:none;position:fixed;inset:0;
     background:rgba(0,0,0,.8);z-index:50;
     color:white;align-items:center;justify-content:center;
     font-size:24px;font-weight:bold;">
            🔍 Finding player… Please wait
        </div>

        <button id="playHuman" style="background:#2563eb;color:white">Play vs Human</button>
        <button id="playComputer" style="background:#16a34a;color:white">Play vs Computer (Medium)</button>
        <button id="resetBtn" style="background:#ef4444;color:white">Reset</button>
        <span id="modeLabel" style="margin-left:12px;color:#fff">Mode: Online</span>
    </div>

    <div class="w-full h-screen flex items-center justify-center">
        <div class="chessboard w-96 h-96 bg-red-400"></div>
    </div>

    <!-- libs -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/socket.io/4.8.1/socket.io.js" integrity="sha512-8BHxHDLsOHx+flIrQ0DrZcea7MkHqRU5GbTHmbdzMRnAaoCIkZ97PqZcXJkKZckMMhqfoeaJE+DNUVuyoQsO3Q==" crossorigin="anonymous" referrerpolicy="no-referrer"></script>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/chess.js/0.10.3/chess.min.js" integrity="sha512-xRllwz2gdZciIB+AkEbeq+gVhX8VB8XsfqeFbUh+SzHlN96dEduwtTuVuc2u9EROlmW9+yhRlxjif66ORpsgVA==" crossorigin="anonymous" referrerpolicy="no-referrer"></script>

    <script src="/js/chessgame.js"></script>
</body>

</html>

</html>